# LensScope V228 基本操作修正版

V228_REALTEST_GEOMETRY_FUSION を基準とする修正。カメラ・レンズDB、Image Circleの出典値、実写のキャリブレーション値は変更していません。公開サイトへのデプロイは行っていません。

## 発見・修正と再現条件

| 問題 | 再現例・修正 |
|---|---|
| 配布ZIPの実写画像未同梱 | 元V228 ZIPはHTML・JSON・監査メモのみ。「Lensscope.zip」からV228が参照する1,281画像を元のパスのまま補完。別ZIPのコード・DBは採用していない。 |
| センサー枠だけ縮められCoverageの縮尺が不正 | ALEXA 265 + Master Prime。スマートフォン幅で枠幅/IC直径が本来1.738なのに1.394。図形全体を同じ倍率で収め、比率を維持。 |
| REAL TESTの正方形・縦長フレームが領域外にはみ出す | ALEXA 35 → 3K 1:1 → REAL TEST。縦横両方でフィットし、画像と枠のGeometryを一致。画面リサイズ時も再計算。 |
| 未撮影焦点距離で内部状態だけREAL TESTが残る | VISTA PrimeをREAL TEST表示 → 21mm。見た目はOPTICAL MAPでも内部はrealだった。描画前に状態を確定し、焦点距離・スライダー・表示を同時更新。 |
| 画像失敗時にも実写表示成功のように見える | 画像リクエスト失敗時にCAPTURE UNAVAILABLEを表示。再試行でき、旧画像の遅延応答で新選択を上書きしない。 |
| 寸法不明モードの枠がカメラ実寸と誤認される | 寸法未登録モードを選択。架空の16:9枠を非表示。REAL TESTの撮影元フォールバックは「選択中カメラの枠ではない」と明記。撮影範囲外も未検証と明記。 |
| カメラ寸法不明で既知のレンズCoverageまで消える | 寸法未登録モード + 数値ICがあるレンズ。レンズ側の既知情報は表示し、カメラ側はDIMENSIONS UNKNOWNを維持。 |
| 旧バージョン表示 | タイトル・ヘッダーのV135表記をV228に修正。 |

## 実施したテスト

Chromiumでアプリを実行。多数組み合わせは画面のピッカーと選択ボタンのDOMクリックで操作し、表示値・実際の描画矩形・相対縮尺を検査。主要なスマートフォン操作はPlaywrightの実クリックも使用。

- OPTICAL MAP：8,174ケース。44カメラの初期モード × 全125選択可能レンズ、および残り全モード × 代表7レンズ。426モードを網羅。
- REAL TEST：1,278ケース（426モード × 球面・ズーム・アナモ代表3レンズ）。同一選択の枠と実写レイヤーのサイズ・縦横比を検証。
- 全1,281参照画像を実際に読み込み。レンズ・焦点距離・OPEN/CLOSEと表示画像の一致を確認。
- スマートフォン幅390px：主要7カメラ × 5レンズ、レンズ先行選択を含む35ケース。再読み込み、縦横回転、デスクトップ幅への変更も確認。
- 寸法不明37モード × 125レンズ＝4,625ケース。架空のカメラ枠を表示せず、既知のレンズICが消えないことを確認。
- HTTPで画像エラー、再試行、旧画像の遅延応答、寸法不明から既知モードへの復帰を検証。
- 最終の各テスト結果はPASS、捕捉したJavaScript例外は0。
- 画面キャプチャを目視確認。テスト環境に日本語フォントがないためキャプチャの日本語が四角で表示される箇所がある。

## 残っている制約

- 37モードにはV228 DB上で有効な物理寸法がないため、Active AreaやCoverage判定は生成しない。数値の推測・追加調査は今回行っていない。
- 未撮影焦点距離の画像は追加できない。既知のS4/i 27mm、THALIA 55mmの欠落画像も素材ZIPにはなかった。
- 元画像の撮影範囲外は実写検証できない。別カメラのセンサースタック等の違いまで実証するものではない。
- iPhone実機/Safariは未検証。モバイル検証はChromiumの画面幅・タッチ設定による。
- 全カメラ×全モード×全レンズの総当たりや、一次情報によるDB監査は今回の対象外。

## 使い方・再実行

ZIPを展開し、index.html と assets/ を同じ階層に保持。既存サイトへ反映する場合もassetsを含めて配置する。data.jsonは従来どおり書き出し用で、画面はindex.html内の同一DATAを両モードで参照する。

テストにはNode.js・Playwright・Chromiumが必要。

```sh
npm install playwright
npx playwright install chromium
node tests/basic-flow.cjs
node tests/unknown-geometry.cjs
node tests/recovery.cjs
```

既存Chromiumを使う場合はBROWSER_EXECUTABLEを指定。tests/*results.jsonに検証結果、V228_BASIC_FLOW_FIX.patchにHTML修正差分を同梱。
